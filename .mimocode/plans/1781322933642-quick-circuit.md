# Plan: 架构冗余清理

## 审查结果：原计划中有 6 项错误判定，已在此版修正

### 错误判定修正
| 原计划项 | 判定 | 修正 |
|----------|------|------|
| `load_optimization_history()` 死代码 | ✗ | 返回值在 line 2133-2134 被使用，**保留** |
| `adjust_grid_spacing()` 死代码 | ✗ | 在 line 2155 被调用且结果在 2160 被日志使用，**保留** |
| `get_version()` 死代码 | ✗ | 在 `strategy.py:2086` 被调用，**保留**（仅 main.py 的 import 多余） |
| `send_notification()` 死代码 | ✗ | 在 `main.py` 被调用 3 次，**保留**（虽为空 stub） |
| `calculate_realized_volatility()` 重复计算 | ✗ | 两次调用使用不同输入（`.iloc[:-1]`），非重复，**保留** |
| `risk.max_position_per_stock` 未使用 | ✗ | 5 个文件引用，**保留** |
| `selection.hurst_threshold` 未使用 | ✗ | `fetcher.py` 中 7 处引用，**保留** |

---

## 执行步骤

### Phase 1: 清理死代码（仅确认无外部调用的函数/类）

1. **删除 `risk_management/enhanced_risk.py` 中 `EnhancedRiskControl` 类**
   - 确认：0 次实例化调用 `EnhancedRiskControl(`
   - `strategy.py:35` 的 import 同时删除

2. **删除 `data_layer/fetcher.py` 中的死函数**：
   - `calculate_volatility()` (~2301) — 仅被 dead 的 `prepare_selection_data` 调用
   - `filter_stocks_by_criteria()` (~2544) — 0 次调用，带 TODO
   - `get_update_strategy()` (~2984) — 仅被 dead 的 `prepare_selection_data` 调用
   - `prepare_selection_data()` (~3019) — 0 次外部调用

3. **删除 `trading_core/grid_engine.py` 中的死函数**：
   - `calculate_grid_profit_potential()` (~626) — 0 次调用
   - `create_grid_report()` (~672) — 0 次调用

4. **删除 `trading_core/strategy.py` 中旧版4参数网格兼容代码**
   - ~2739-2748：`if 'grid_spacing' in params and 'buy_spacing_pct' not in params`
   - 确认：`config_state.json` 中不存在旧格式参数

5. **删除 `utils/utils.py` 中的死函数**：
   - `check_trading_time()` (~124) — 0 次调用（已导出但从未使用）
   - `format_number()` (~271) — 0 次调用（被 `strategy.py` import 但从未使用，该 import 也删除）

6. **删除 `data_layer/tencent_fetcher.py` 中 `update_stocks_data()`** — 0 次调用

7. **删除 `data_layer/eastmoney_fetcher.py` 中 `update_stocks_from_eastmoney()`** — 0 次调用

### Phase 2: 合并重复逻辑

1. **删除 `utils/utils.py` 中的 `check_t1_rule()` 和 `enhanced_risk.py` 中的 `can_sell()`**
   - 两者均为死代码（0 次调用），非有效重复。直接删除，无需保留。

2. **统一涨跌停检查（3-way duplicate）**
   - 3 份相同实现：`utils/utils.py:check_limit_status` / `grid_engine.py:check_limit_status` / `enhanced_risk.py:check_limit_status`
   - 仅 `grid_engine.py` 版本有 1 个调用者（`generate_signals()` line 507）
   - **操作**：删除 `utils/utils.py` 和 `enhanced_risk.py` 中的拷贝，保留 `grid_engine.py` 版本

3. **⚠️ 费用计算：不合并**
   - `utils/utils.py:calculate_transaction_fee()` 被 backtest 路径调用 5 次
   - `enhanced_risk.py:calculate_fees()` 被 live 路径调用 1 次
   - 两者有不同特性（ETF 豁免 vs 参数化费率），合并风险高。**跳过此项**

4. **⚠️ 波动率计算：不合并**
   - `fetcher.py:calculate_volatility()` 被 `prepare_selection_data()` 调用（但该函数将被删除）
   - `indicators.py:calculate_volatility_60d()` 被主指标管线调用
   - 删除 `fetcher.py` 版本后（Phase 1 第2项），此重复自然消除。**无需额外操作**

5. **删除 `grid_engine.py` 中 DEPRECATED `calculate_volatility_adjusted_spacing()`**
   - 确认：0 次调用，已被 `compute_adaptive_spacing()` 取代（有 4 个调用者）

### Phase 3: 清理遗留文件和目录

1. **删除 `simulation/` 目录** — 无 .py 文件，仅含旧模块的 `__pycache__`

2. **删除根目录 `EOF` 文件** — 0 字节 artifact

3. **清理 `__pycache__` 目录** — 各子目录下均有，已在 `.gitignore` 中但部分被 git 跟踪，`git rm -r --cached` 移除

4. **添加 `.pytest_cache/` 到 `.gitignore` 并清理** — 当前未被忽略

5. **移动 `test_rate_limit_fix.py` 到 `tests/`** — 根目录不应有测试文件

### Phase 4: 修复依赖和配置

1. **更新 `requirements.txt`**：
   - 移除 `scipy>=1.10.0`（0 次 `import scipy`）
   - 添加 `numba>=0.59.0`（`indicators.py` 使用）
   - 添加 `gradio>=4.0.0`（`dashboard.py` 使用）
   - 添加 `plotly>=5.0.0`（`dashboard.py` 使用，可选链入 gradio 但显式导入）
   - 添加 `tushare>=1.4.0`（`fetcher.py` 条件导入）
   - 添加 `fake-useragent>=1.5.0`（`session_manager.py` 条件导入）

2. **更新 `environment.yml`**：
   - 同步 `requirements.txt` 变更
   - 添加 `baostock>=0.8.8`（当前仅在 requirements.txt）

3. **清理 `config.yaml` 中确认未使用的配置节**：
   - 移除 `screening.factor_weights`（被 `advanced_screening.weights` 取代，代码只读后者）
   - 移除 `large_capital`（0 次代码引用）
   - 移除 `pre_filter`（0 次代码引用，函数名 `pre_filter_stocks` 不读取此配置）
   - 移除 `asset_classification`（0 次代码引用）
   - 移除 `slippage`（代码使用 `backtest.slippage_rate`，enhanced_risk 有硬编码 `SLIPPAGE_MODEL`）
   - 移除 `error_recovery`（0 次代码引用，session_manager 硬编码默认值）
   - 移除 `http`（0 次代码引用，session_manager 硬编码默认值）
   - ❌ ~~移除 `risk.max_position_per_stock`~~ — **保留，5 个文件使用**
   - ❌ ~~移除 `selection.hurst_threshold`~~ — **保留，fetcher.py 使用**
   - 移除 `trading_calendar` 中未使用的 `verify_on_load`, `cross_validate`, `warn_on_mismatch`（0 次引用）
   - 移除 `data_sources.akshare.timeout`（0 次引用，未传递给 fetch 函数）
   - 移除 `data_sources.baostock.auto_login`（0 次引用，`_ensure_baostock_login` 总是自动登录）

4. **删除 `fetcher.py` 中旧的 JSON 文件引用**：
   - `METADATA_FILE` (~539)：已在 Phase 1 删除 `prepare_selection_data` 后检查是否仍有引用
   - `_CHECKPOINT_FILE` (~71)：检查 `smart_download_or_update` 是否仍使用
   - `_CANDIDATE_FILE` (~74)：同上

5. **修复测试文件中的旧版 import 路径**：
   - `tests/test_stock_selection.py`：`from data import ...` → `from data_layer.fetcher import ...`
   - `tests/test_stock_selection.py`：`from strategy import ...` → `from trading_core.strategy import ...`
   - `tests/test_listing_duration.py`：`from strategy import ...` → 更新
   - `tests/test_incremental_update.py`：`from data import ...` → 更新

### Phase 5: 验证

1. 运行 `python -m pytest tests/` — 所有测试应通过
2. 运行 `python main.py --version` — 应正常输出版本
3. 运行 `python -c "from trading_core.strategy import execute_strategy"` — 应无导入错误
4. 运行 `python -c "from data_layer.fetcher import get_stock_data"` — 应无导入错误

## 关键文件清单

| 文件 | 操作 |
|------|------|
| `risk_management/enhanced_risk.py` | 删除 EnhancedRiskControl 类 + check_limit_status + can_sell |
| `data_layer/fetcher.py` | 删除 4 个死函数 + 旧 JSON 常量 |
| `data_layer/tencent_fetcher.py` | 删除 update_stocks_data() |
| `data_layer/eastmoney_fetcher.py` | 删除 update_stocks_from_eastmoney() |
| `trading_core/strategy.py` | 删除兼容代码 + 清理未使用的 import |
| `trading_core/grid_engine.py` | 删除 3 个死/弃用函数 |
| `utils/utils.py` | 删除 check_trading_time, format_number, check_t1_rule, check_limit_status |
| `configuration/config.yaml` | 删除 8 个未使用配置节/字段 |
| `requirements.txt` | 移除 scipy, 添加 5 个缺失依赖 |
| `environment.yml` | 同步 requirements.txt + 添加 baostock |
| `.gitignore` | 添加 .pytest_cache/ |
| `tests/*.py` | 修复旧版 import 路径 |
| `simulation/` | 删除整个目录 |
| `EOF` | 删除 |
| `__pycache__/` (各子目录) | git rm --cached |
| `.pytest_cache/` | git rm --cached |

## 风险与注意事项

- **`EnhancedRiskControl` 删除**：若 `enhanced_risk.py` 中 `calculate_fees` 被 `record_trade` 内部使用，需保留 `EnhancedRiskControl` 类框架（`record_trade` 依赖 `self.positions` 等实例属性）
- **费用计算不合并**：`calculate_transaction_fee`（backtest 路径 5 次调用）和 `calculate_fees`（live 路径 1 次调用）有不同特性，合并风险高，跳过
- **测试 import 修复**：旧测试可能依赖不存在的旧扁平模块，修复时需确认新模块中对应函数名和签名未变化
- **JSON 常量删除**：需确认 `load_metadata`/`save_metadata` 未被 `smart_download_or_update` 或其他活跃路径调用
